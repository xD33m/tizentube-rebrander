# Build artifacts (generated by rebrand.sh)
/build/

# Signed APKs, one per app and release tag
/dist/

# Downloaded upstream APKs, keyed by app and release tag
/.cache/

# Java / tool jars are bundled intentionally — don't ignore tools/
# But do ignore any keystore files
*.keystore
*.jks

# OS junk
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo
